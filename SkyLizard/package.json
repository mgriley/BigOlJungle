{
  "name": "skylizard",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build-image": "docker build -t sky-lizard-ubuntu .",
    "docker-run": "docker run --rm -it -v $(pwd):/app -w /app sky-lizard-ubuntu node src/main.js",
    "local": "nodemon --watch src src/main.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
